<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>ToDo</title>
  </head>
  <body>
    <h1>To Do Broski</h1>

    <form class="" action="/" method="post">
      <input type="text" name="description" value="" placeholder="add a todo">
      <button type="submit" >Add todo</button>
    </form>

    <ul>
      {{#todoListForTheBrowser}}
      <li>
        <form class="" action="/remove" method="post">
          <button type="submit" name="markCompleted" value="{{.}}">Mark Complete</button>
          {{.}}
        </form>
      </li>

      {{/todoListForTheBrowser}}
    </ul>

    <h2>Completed tasks</h2>
      <ul>
        {{#completedListForBrowser}}
        <li>{{.}}</li>
        {{/completedListForBrowser}}
      </ul>


  </body>
</html>
